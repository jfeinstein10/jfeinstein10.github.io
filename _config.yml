# This is the default format.
# For more see: https://github.com/mojombo/jekyll/wiki/Permalinks
permalink: /blog/:title

exclude: [".rvmrc", ".rbenv-version", "README.md", "Rakefile", "Gemfile", "Gemfile.lock", "package.json"]
highlighter: pygments

# Themes are encouraged to use these universal variables
# so be sure to set them if your theme uses them.
#
title : Jeremy Feinstein
tagline: Ramblings
author :
  name : Jeremy Feinstein
  email : j.feinstein10@gmail.com
  github : jfeinstein10
  twitter : jemfeinstein

sass:
  load_paths:
    - css
    - _sass
  style: compressed

gems:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jemoji
  - octopress-paginate
